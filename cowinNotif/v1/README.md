### once downloaded, change district code. minimum age and retry time in Program.cs

### dotnet restore
### to build an exe, type command "dotnet publish --configuration Release"
### double click exe to run the app